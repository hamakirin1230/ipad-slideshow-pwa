# 2026-06-04 第4-3-2 Google Photos Picker選択まで 引き継ぎメモ

このファイルは、iPad用スライドショーPWA制作プロジェクト `ipad-slideshow-pwa` の次チャット引き継ぎ用メモです。

## 1. リポジトリ

```text
hamakirin1230/ipad-slideshow-pwa
```

## 2. 現在のフェーズ

```text
第4ゴール / 第4-3 素材追加・assets保存
現在の作業スライス: 第4-3-2 Google Photos Picker選択まで
```

## 3. このチャットで完了したこと

直前チャットでは、第4-3-2の実装前設計グリルを質問47〜155まで実施した。

その後、第4-3-2の小分け実装方針に従い、次の2つのパッチを適用した。

### 第1パッチ: decision doc更新

更新ファイル:

```text
docs/decisions/goal-04-3-asset-import.md
```

内容:

```text
- 第4-3-2の目的とスコープを追記
- Drive保存・manifest更新は第4-3-2では行わない境界を明記
- Photos権限つきtokenの扱いを明記
- Picker session lifecycle を明記
- selected / cancelled / invalid / error の意味を明記
- 画像bytesをReact stateに保持しない方針を明記
- Content-Type検証方針を明記
- 10MB上限検証方針を明記
- 中止・cleanup方針を明記
- UI表示方針を明記
- 実装分割方針を明記
```

### 第2パッチ: Photos Picker lib土台追加

追加ファイル:

```text
src/lib/google-photos-picker.ts
```

内容:

```text
- Photos Picker API endpoint定数
- createPhotosPickerSession()
- getPhotosPickerSession()
- deletePhotosPickerSession()
- listPickedMediaItems()
- extractSinglePickedMediaItem()
- fetchAndValidatePickedPhoto()
- normalizeCreatedPickingSessionResponse()
- normalizePickingSessionSnapshotResponse()
- normalizePickedMediaItemsResponse()
- normalizePickedMediaItem()
- normalizeContentType()
- isPhotosDownloadedAssetMimeType()
- parseGoogleDurationSeconds()
- resolvePollingTiming()
- PhotosPickerApiError
- PhotosPickerSelectionError
```

この第2パッチでは、まだ `AppProviders` や `/admin` UI には接続していない。

## 4. 検証結果

ユーザーがローカルで以下を実行し、成功済み。

```text
npm run lint
npm run build
```

build結果:

```text
Next.js 16.2.6 (Turbopack)
Compiled successfully
Finished TypeScript
Generated static pages

Route (app)
- /
- /_not-found
- /admin
- /auth-test
- /player
- /settings
```

その後、GitHub Desktopで Commit → Push を実行し、GitHub Actions deploy も完了済み。

コミット名は、以下の趣旨で実施済み。

```text
Add Photos Picker selection foundation
```

正確には、次チャットでGitHub上の最新コミットを確認すること。

## 5. 第4-3-2で確定した主な設計決定

### 5.1 第4-3-2の範囲

第4-3-2では、Google Photos Pickerで1件を選択し、表示用画像bytesを取得し、形式とサイズを検証するところまでを扱う。

第4-3-2で行う:

```text
- Photos権限つきtoken取得
- Picker session作成
- pickingConfig.maxItemCount: "1"
- pickerUri + "/autoclose" を別ウィンドウまたは別タブで開く
- pollingConfigに従って session を待つ
- mediaItems.list の結果を1件として検証
- mediaItem.id / type / mediaFile.baseUrl / mediaFile.mimeType / createTime を検証
- baseUrl + "=w2732-h2732" から画像bytesを取得
- 取得レスポンスの Content-Type を検証
- 10MB上限を検証
- /admin に取得結果と診断を表示
- Photos Picker session を後片付けする
```

第4-3-2で行わない:

```text
- Drive assets/ upload
- Drive asset metadata検証
- manifest.json 再読込
- manifest.json.slides[] append
- manifest.json 保存
- 保存後のmanifest再読込検証
- projectSummary / projectDetails の楽観更新
- 画像プレビュー表示
- IndexedDB同期
- オフライン本番再生
```

### 5.2 token方針

通常のGoogle接続は `drive.file` のまま。

素材追加開始時だけ、次のscopeを一時的に要求する。

```text
drive.file
photospicker.mediaitems.readonly
```

Photos権限つきtoken要求では次を使う。

```text
scope: DRIVE_AND_PHOTOS_PICKER_SCOPES
include_granted_scopes: true
prompt: "consent"
```

Photos権限つきtokenは素材追加フロー内だけで使う。

```text
- accessTokenRef には保存しない
- googleStatus は更新しない
- googleMessage は更新しない
- driveFileGranted は更新しない
- Drive状態はリセットしない
- project状態はリセットしない
```

scope確認は、既存の `hasGrantedDriveFileAndPhotosPickerScopes()` に一本化する。

### 5.3 Picker session lifecycle

session作成bodyは以下のみ。

```json
{
  "pickingConfig": {
    "maxItemCount": "1"
  }
}
```

静止画像だけを選ばせる追加フィールドは入れない。

静止画像かどうかは取得後に `mediaItem.type === "PHOTO"` で検証する。

完了条件は `sessions.get` の `mediaItemsSet === true`。

完了後に `mediaItems.list?pageSize=2` を1回だけ呼ぶ。

結果の扱い:

```text
0件:
- cancelled

1件 かつ nextPageTokenなし:
- metadata検証へ進む

2件以上:
- invalid

nextPageTokenあり:
- invalid
```

作成済みsessionは、成功・キャンセル・invalid・errorのどの場合でも `sessions.delete` を試す。

`sessions.delete` の失敗は主処理結果を上書きしない。

### 5.4 状態モデル

第4-3-2で `AssetImportStatus` に `selected` を正式追加する。

`selected` の意味:

```text
- Google Photos Pickerで1件選択済み
- mediaItem metadata検証済み
- 表示用画像bytes取得済み
- Content-Type検証済み
- 10MB上限検証済み
- Drive保存: 未実行
- manifest反映: 未実行
```

`completed` は第4-3-3以降まで温存する。

`completed` は、Drive assets/ 保存、manifest.json.slides[] 反映、Drive再読込・再検証まで成功した状態として扱う。

分類:

```text
selected:
- Photosから1件取得し、形式とサイズ検証まで成功

cancelled:
- Pickerで0件
- Picker polling timeout
- Photos権限待ちtimeout
- ユーザー中止
- Picker用タブが遷移前に閉じられた
- access_denied

invalid:
- 選択結果が2件以上
- nextPageTokenあり
- PHOTO以外
- mediaItem必須項目不正
- Content-Type不許可
- 10MB超過
- 開始条件違反

error:
- token取得の予期しない失敗
- scope不足
- session作成失敗
- sessions.get失敗
- mediaItems.list失敗
- 画像bytes取得失敗
- popup blocked
- 通信・認証・API失敗
```

### 5.5 assetImportSelection方針

第4-3-2では、成功結果を `assetImportSelection` に保持する。

保持する情報:

```text
- mediaItemIdPart
- mediaItemType
- filename
- sourceMimeType
- sourceCreateTime: string | null
- downloadedContentType
- downloadedSizeBytes
- sizeLimitBytes
- driveSaved: false
- manifestUpdated: false
```

保持しない情報:

```text
- access_token
- Authorization header
- mediaItem.id の全文
- mediaFile.baseUrl
- baseUrl + "=w2732-h2732" のURL
- pickerUri
- sessionId
- Blob
- ArrayBuffer
- Uint8Array
- object URL
```

`assetImportSelection` は完全成功時だけセットする。

`cancelled / invalid / error` では常に `null` にする。

### 5.6 画像bytesと検証

画像取得URL:

```text
mediaFile.baseUrl + "=w2732-h2732"
```

使わない:

```text
-c
d
```

取得時はPhotos権限つきtokenをAuthorization headerに付ける。

```text
Authorization: Bearer <Photos権限つきtoken>
```

fetchには以下を明示する。

```text
cache: "no-store"
credentials: "omit"
```

Content-Typeは正規化し、以下だけ許可する。

```text
image/jpeg
image/png
image/webp
```

10MB上限はPhotos元ファイルではなく、実際に取得した表示用画像bytesに対して適用する。

内部上限:

```text
10 * 1024 * 1024 = 10485760 bytes
```

`downloadedBytes > sizeLimitBytes` の場合に超過。

ちょうど10MBは許容。

### 5.7 pollingとtimeout

Picker待機全体には既存の15秒Drive操作timeoutを使わない。

Photos Picker用に独立した待機制御を持つ。

Google Duration文字列は専用パーサーで厳密に読む。

`pollInterval`:

```text
- 有効ならその値
- 不正なら3秒
- 1秒未満なら1秒に丸める
```

`timeoutIn`:

```text
- 有効ならその値
- 0s なら即 cancelled
- 不正なら120秒
- 300秒超なら300秒に制限
```

`sessions.get` ごとに最新の `pollingConfig` を読み直す。

ただし、総待機時間は開始時刻から最大300秒で打ち切る。

polling待機はAbortSignal対応の `abortableSleep()` で実装する。

### 5.8 排他制御と中止

素材追加専用の状態を新設する。

```text
assetImportInFlightRef
isAssetImportInFlight
assetImportAbortRef
assetImportRequestIdRef
```

Drive操作とは内部的に分離する。

UI上は、Drive操作と素材追加を相互排他にする。

中止ボタンは処理中だけ表示する。

中止ボタン文言は常に:

```text
素材追加を中止
```

中止時は、Driveやmanifestには触らない。

### 5.9 UI方針

`AssetImportPanel` に実行可能な「素材を追加」ボタンを追加する。

`canImportAssets` はproject ready導線条件のまま維持する。

実行可能条件は `canStartAssetImport` として別に計算する。

```text
canImportAssets:
- projectStatus === "ready"
- driveProjectReadyContext !== null

canStartAssetImport:
- canImportAssets === true
- isDriveOperationInFlight === false
- isAssetImportInFlight === false
- projectDetails !== null
- projectDetails.slideCount < 50
```

`canStartAssetImport` と `assetImportBlockedReason` は `AppProviders` で一元計算する。

開始ボタン文言:

```text
idle:
- 素材を追加

selected:
- 別の写真を選ぶ

cancelled:
- もう一度選択

invalid:
- 別の写真を選ぶ

error:
- もう一度試す

requestingPhotosPermission / openingPicker / waitingForSelection / downloadingFromPhotos:
- 素材追加処理中
```

保存を示唆する文言は使わない。

### 5.10 プレビュー方針

第4-3-2では画像プレビューを表示しない。

完成版ではプレビューを実装したい。

ただし、完成版のプレビューはGoogle Photosの `baseUrl` ではなく、Drive保存済みassetを元に表示する。

### 5.11 静的PWAとしての通信方針

このプロジェクトは静的export前提。

第4-3-2では、Photos Picker APIと画像bytes取得はブラウザからGoogle APIへ直接fetchする。

使わない:

```text
- Next.js API route
- server action
- server proxy抽象化
```

## 6. 次に進める作業

次チャットでまずGitHub上の最新ファイルを確認すること。

特に確認するファイル:

```text
docs/decisions/goal-04-3-asset-import.md
src/lib/google-photos-picker.ts
src/app/app-providers.tsx
src/app/admin/asset-import-panel.tsx
src/app/admin/drive-project-workspace-panel.tsx
src/app/admin/project-status-panel.tsx
src/lib/google-auth.ts
src/lib/google-drive.ts
```

次に進めるパッチは **第3パッチ**。

## 7. 第3パッチの予定

対象ファイル:

```text
src/app/app-providers.tsx
```

入れる予定:

```text
- AssetImportStatus に selected を追加
- assetImportStatusLabels に selected を追加
- AssetImportSelection 型を AppProviders 側に追加
- assetImportSelection state を追加
- isAssetImportInFlight state を追加
- canStartAssetImport を追加
- assetImportBlockedReason を追加
- startAssetImport() を追加
- cancelAssetImport() を追加
- pendingPhotosTokenRequestRef を追加
- currentAssetImportAccessTokenRef を追加
- currentAssetImportSessionIdRef を追加
- assetImportAbortRef を追加
- assetImportRequestIdRef を追加
- Photos token callback 分岐を追加
- Photos token timeout 120秒を追加
- cleanupPhotosPickerSessionOnce() を追加
- sanitizeAssetImportDiagnostics() を追加
- abortableSleep() を追加
- waitForPhotosPickerSelection() を追加
```

第3パッチでは、まだ `AssetImportPanel` のUI変更は最小限または未実施でよい。

ただし、Contextに `startAssetImport / cancelAssetImport / canStartAssetImport / assetImportBlockedReason / isAssetImportInFlight / assetImportSelection` を公開する必要がある。

第4パッチで `AssetImportPanel` にボタン・中止ボタン・結果summary表示を入れる。

## 8. 注意点

第3パッチで絶対に守ること:

```text
- Photos権限つきtokenを accessTokenRef に保存しない
- Photos token成功時に Drive/project状態をリセットしない
- Photos token失敗時に通常Google接続状態を壊さない
- Drive assets/ upload をしない
- manifest.json を読まない
- manifest.json を更新しない
- projectSummary / projectDetails を楽観更新しない
- 画像bytesをReact stateに保存しない
- baseUrl / pickerUri / full mediaItem.id / token をUIやdiagnosticsに出さない
```

第3パッチで `google-drive.ts` は原則触らない。

第3パッチで `google-auth.ts` も原則触らない。

既存の `DRIVE_AND_PHOTOS_PICKER_SCOPES` と `hasGrantedDriveFileAndPhotosPickerScopes()` を使う。

## 9. 次チャット冒頭での推奨プロンプト

```text
このチャットは、iPad用スライドショーPWA制作プロジェクト（ipad-slideshow-pwa）の続きです。

リポジトリ:
hamakirin1230/ipad-slideshow-pwa

現在のフェーズ:
第4ゴール / 第4-3-2 Google Photos Picker選択まで

直前チャットで完了したこと:
- 第4-3-2の設計グリルを質問47〜155まで実施
- docs/decisions/goal-04-3-asset-import.md を第4-3-2設計確定版に更新
- src/lib/google-photos-picker.ts を新規追加
- npm run lint 成功
- npm run build 成功
- GitHub DesktopでCommit → Push済み
- GitHub Actions deploy 完了

次に進めたいこと:
第4-3-2 第3パッチとして、src/app/app-providers.tsx に素材追加フローの状態管理と Photos Picker連携呼び出しを追加したいです。

まずGitHub上の最新ファイルを確認してください。
特に以下を確認してください。
- docs/decisions/goal-04-3-asset-import.md
- src/lib/google-photos-picker.ts
- src/app/app-providers.tsx
- src/app/admin/asset-import-panel.tsx
- src/lib/google-auth.ts
- src/lib/google-drive.ts

そのうえで、実装前に第3パッチの設計を短くグリルし、壊れにくい置換単位で進めてください。
```
